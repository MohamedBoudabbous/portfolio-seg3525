export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-section" aria-labelledby="app-title">
        <p className="eyebrow">Memory Training Interface</p>

        <h1 id="app-title">NeuroMatch</h1>

        <p className="hero-description">
          An interactive memory game designed to train visual memory, attention,
          and sequence recall.
        </p>

        <button className="primary-button" type="button">
          Start building
        </button>
      </section>
    </main>
  );
}